{
  "name": "nexus-market-cloudflare",
  "version": "1.0.0",
  "description": "NEXUS Market — Cloudflare Pages Functions",
  "private": true,
  "scripts": {
    "dev":    "wrangler pages dev . --port 8788 --compatibility-flag nodejs_compat",
    "deploy": "wrangler pages deploy .",
    "secret": "wrangler pages secret put",
    "start":  "wrangler pages dev . --port 8788 --compatibility-flag nodejs_compat"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.0",
    "nodemailer": "^6.9.0",
    "stripe": "^16.12.0",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  },
  "engines": {
    "node": ">=18"
  }
}
