nexus-market/
│
├── functions/
│   ├── api/
│   │   ├── orders/
│   │   │   ├── **create.js**          # Créer une commande
│   │   │   ├── **list.js**            # Lister les commandes (client/vendeur)
│   │   │   ├── **[orderId]/**          # Dossier pour les actions par commande
│   │   │   │   ├── **status.js**      # Mettre à jour le statut
│   │   │   │   └── **cancel.js**       # Annuler une commande
│   │   │   └── **track.js**           # Suivre une commande (intégration livraison)
│   │   │
│   │   ├── notifications/
│   │   │   ├── **send-push.js**       # Envoyer des notifications push (FCM)
│   │   │   └── **send-email.js**      # Envoyer des emails (Nodemailer)
│   │   │
│   │   └── _lib/
│   │       └── **utils.js**            # Fonctions utilitaires (déjà existant)
│   │
│   └── _middleware.js                 # Middleware global
│
├── database/
│   └── migrations/
│       └── **orders_table.sql**       # Schéma de la table `orders`
│
└── docs/
    └── **ORDER_WORKFLOW.md**          # Documentation du flux de commande