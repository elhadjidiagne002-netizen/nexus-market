# ─── netlify.toml — NEXUS Market ────────────────────────────────────────────

[build]
  publish   = "."
  command   = ""
  functions = "netlify/functions"

[build.environment]
  NODE_ENV    = "production"
  PAYTECH_ENV = "prod"

[functions]
  node_bundler = "esbuild"

# ── Redirects ─────────────────────────────────────────────────────────────────

# Paiements (existants)
[[redirects]]
  from   = "/api/payments/mobile-money"
  to     = "/.netlify/functions/payments-mobile-money"
  status = 200
  force  = true

[[redirects]]
  from   = "/api/payments/webhook"
  to     = "/.netlify/functions/paytech-webhook"
  status = 200
  force  = true

# Retraits vendeur PayTech
[[redirects]]
  from   = "/api/payout/request"
  to     = "/.netlify/functions/payout-request"
  status = 200
  force  = true

[[redirects]]
  from   = "/api/payout/history"
  to     = "/.netlify/functions/payout-history"
  status = 200
  force  = true

[[redirects]]
  from   = "/api/payout/webhook"
  to     = "/.netlify/functions/paytech-payout-webhook"
  status = 200
  force  = true

# Push notifications
[[redirects]]
  from   = "/api/push/vapid-key"
  to     = "/.netlify/functions/push-vapid-key"
  status = 200

[[redirects]]
  from   = "/api/push/subscribe"
  to     = "/.netlify/functions/push-subscribe"
  status = 200

[[redirects]]
  from   = "/api/push/send"
  to     = "/.netlify/functions/push-send"
  status = 200

# SPA fallback — doit rester en dernier
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200

# ── Headers ────────────────────────────────────────────────────────────────────

[[headers]]
  for = "/index.html"
  [headers.values]
    Cache-Control          = "no-cache, no-store, must-revalidate"
    X-Frame-Options        = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy        = "strict-origin-when-cross-origin"
    Permissions-Policy     = "camera=(), microphone=(), geolocation=()"

[[headers]]
  for = "/"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"

[[headers]]
  for = "/.netlify/functions/*"
  [headers.values]
    Cache-Control                = "no-store"
    Access-Control-Allow-Origin  = "*"
    Access-Control-Allow-Methods = "POST, GET, DELETE, OPTIONS"
    Access-Control-Allow-Headers = "Content-Type, Authorization"

[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/*.woff2"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/sw.js"
  [headers.values]
    Cache-Control = "no-cache, no-store"
