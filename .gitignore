# ── Secrets — ne JAMAIS envoyer sur GitHub ───
.env
.env.backup
.env.local
.env.*.local

# ── Dépendances npm ───────────────────────────
node_modules/
npm-debug.log*

# ── Logs ──────────────────────────────────────
*.log
logs/

# ── Système Windows ───────────────────────────
Thumbs.db
desktop.ini

# ── Fichier reset démo (dangereux en prod) ────
nexus_reset.html
# -- Variantes .env sensibles --------------------
_env
env.corrige
env.example
.env.example
*.env
# -- Fichiers de d�ploiement sensibles -----------
DEPLOYMENT.md
nexus-start.ps1
# -- Fichiers SQL (sch�mas internes) -------------
*.sql
# -- Scripts serveur annexes ---------------------
server_analytics_addon.js
*.bak
*.html.bak
